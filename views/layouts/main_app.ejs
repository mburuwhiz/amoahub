<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title : 'Amora Hub' %></title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/app_style.css">
    <link rel="icon" href="/favicon.ico" type="image/x-icon">
</head>
<body>
    <header class="app-header">
        <div class="container">
            <nav class="main-app-nav">
                <!-- Mobile Menu Trigger (Hamburger) - Moved to the left -->
                <div class="mobile-menu-trigger">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="3" y1="12" x2="21" y2="12"></line><line x1="3" y1="6" x2="21" y2="6"></line><line x1="3" y1="18" x2="21" y2="18"></line></svg>
                </div>

                <!-- Logo - Stays on the left, but after hamburger on mobile -->
                <a href="/discover" class="app-logo">Amora Hub</a>

                <!-- Desktop Navigation Links - Centered -->
                <div class="nav-links-main">
                    <a href="/discover">Discover</a>
                    <a href="/matches">Matches</a>
                    <a href="/chats">Chats</a>
                </div>

                <!-- User Profile Menu - Moved to the right -->
                <div class="user-menu-container">
                    <div class="user-menu-trigger">
                        <img src="<%= user.profileImage %>" alt="Your profile" class="user-avatar">
                    </div>
                    <div class="user-dropdown">
                        <a href="/profile">My Profile</a>
                        <a href="/notifications">Notifications</a>
                        <a href="/profile/edit">Edit Profile</a>
                        <a href="/logout">Logout</a>
                    </div>
                </div>
            </nav>
        </div>
    </header>
    <main class="app-main-content">
         <%- include('../partials/_messages') %>
        <%- body %>
    </main>
    <script src="/socket.io/socket.io.js"></script>
    <script src="/js/main.js"></script>
</body>
</html>