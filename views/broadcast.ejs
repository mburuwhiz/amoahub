<div class="broadcast-container">
    <h1>📢 Announcement from Amora Hub</h1>
    <div class="broadcast-card">
        <p class="broadcast-message"><%= broadcast.message %></p>
        <div class="broadcast-footer">
            <span>Sent by <%= broadcast.admin.displayName %> on <%= new Date(broadcast.createdAt).toLocaleDateString() %></span>
        </div>
    </div>
    <a href="/notifications" class="back-link">Back to Notifications</a>
</div>

<style>
.broadcast-container {
    max-width: 800px;
    margin: 2rem auto;
    padding: 2rem;
}
.broadcast-card {
    background: #fff;
    padding: 2rem;
    border-radius: 12px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.08);
    margin-top: 1.5rem;
    border: 1px solid var(--border-color);
}
.broadcast-message {
    font-size: 1.1rem;
    line-height: 1.7;
    white-space: pre-wrap; /* This preserves line breaks from the textarea */
}
.broadcast-footer {
    margin-top: 2rem;
    text-align: right;
    font-size: 0.9rem;
    color: #666;
}
.back-link {
    display: inline-block;
    margin-top: 2rem;
    color: var(--primary-color);
    text-decoration: none;
    font-weight: 600;
}
</style>